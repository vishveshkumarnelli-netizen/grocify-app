{
  "name": "grocify",
  "version": "1.0.0",
  "description": "Grocify - Full Stack Grocery E-Commerce (MERN + Tailwind)",
  "scripts": {
    "dev":         "concurrently \"npm run server\" \"npm run client\" \"npm run admin\"",
    "dev:no-admin":"concurrently \"npm run server\" \"npm run client\"",
    "server":      "cd server && npm run dev",
    "client":      "cd client && npm run dev",
    "admin":       "cd admin  && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install && cd ../admin && npm install",
    "seed":        "cd server && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
